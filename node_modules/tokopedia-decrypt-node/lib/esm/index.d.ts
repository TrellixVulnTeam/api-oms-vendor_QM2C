import crypto from 'crypto';
interface ContentPayload {
    secret: string;
    content: string;
}
declare const decryptContent: {
    (privateKey: crypto.KeyLike, { secret, content }: ContentPayload): any;
    default: any;
};
export = decryptContent;
